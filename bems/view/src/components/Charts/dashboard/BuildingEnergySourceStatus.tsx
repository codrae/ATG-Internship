/*
import React from "react";
import EnergyChart from "@/components/Charts/EnergyChart";
import { buildingsDict } from "@/data/buildings";

const BuildingEnergySourceStatus: React.FC = () => {
  return (
    <div className="bg-white shadow-lg rounded-md p-4">
      <EnergyChart
        building_ids={Object.keys(buildingsDict)}
        energy_types={["POWER"]}
        label="건물별 에너지원 사용 현황"
      />
    </div>
  );
};

export default BuildingEnergySourceStatus;
*/
